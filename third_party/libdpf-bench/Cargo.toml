[package]
name = "libdpf-bench"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
libdpf = { path = "../libdpf/libdpf-rust" }

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "bench"
harness = false
